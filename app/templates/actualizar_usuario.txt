<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actualizar Usuario</title>
    <!-- Bootstrap CSS desde CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center text-primary">Actualizar Usuario</h1>

        <!-- Formulario para buscar usuario -->
        <form method="POST" action="/update_user" class="mt-4">
            <div class="mb-3">
                <label for="buscar" class="form-label">Buscar por número de teléfono o nombre</label>
                <input type="text" class="form-control" id="buscar" name="buscar" placeholder="Ingresa el número o el nombre del usuario" required>
            </div>
            <div class="d-grid">
                <button type="submit" class="btn btn-primary">Buscar Usuario</button>
            </div>
        </form>

        {% if usuario_encontrado %}
        <!-- Formulario para actualizar datos si se encuentra el usuario -->
        <hr class="my-4">
        <h2 class="text-center text-success">Modificar Datos del Usuario</h2>
        <form method="POST" action="/update_user/confirm" class="mt-4">
            <!-- Nombre -->
            <div class="mb-3">
                <label for="nombre" class="form-label">Nombre</label>
                <input type="text" class="form-control" id="nombre" name="nombre" value="{{ usuario_encontrado['nombre'] }}" placeholder="Ingresa un nuevo nombre (opcional)">
            </div>
            <!-- Teléfono -->
            <div class="mb-3">
                <label for="telefono" class="form-label">Teléfono</label>
                <input type="tel" class="form-control" id="telefono" name="telefono" value="{{ usuario_encontrado['telefono'] }}" placeholder="Ingresa un nuevo teléfono (opcional)">
            </div>
            <!-- Dirección -->
            <div class="mb-3">
                <label for="direccion" class="form-label">Dirección</label>
                <input type="text" class="form-control" id="direccion" name="direccion" value="{{ usuario_encontrado['direccion'] }}" placeholder="Ingresa una nueva dirección (opcional)">
            </div>
            <!-- Botón de aceptar -->
            <div class="d-grid">
                <button type="submit" class="btn btn-success">Aceptar</button>
            </div>
        </form>
        {% endif %}
    </div>
    <!-- Bootstrap JS desde CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
